import 'package:flutter/cupertino.dart';

class Employees extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('13',style: TextStyle(fontSize: 50),),
    );
  }
}