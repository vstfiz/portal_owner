import 'package:flutter/cupertino.dart';

class Customers extends StatelessWidget{

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('14',style: TextStyle(fontSize: 50),),
    );
  }
}